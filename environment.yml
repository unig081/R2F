name: zxc_r2f
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch>=2.4
  - pytorch-cuda=12.1
  - numpy
  - pyyaml
  - tqdm
  - safetensors
  - pip:
      - accelerate>=1.1.0
      - transformers>=4.45.0
      - peft>=0.13.0
      - datasets>=3.0.0
      - bitsandbytes>=0.44.0
      - sentencepiece
      - protobuf
      - pandas
