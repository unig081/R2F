# TOFU 相关指标整理



## 1. FE

**指标：**  
FE，Forget Efficacy，遗忘有效性。

**意义：**  
衡量模型在 **forget set** 上是否真的忘掉目标知识，同时避免模型通过输出乱码来虚假提高遗忘效果。FE 越高，表示遗忘效果越好。  

**公式：**

\[
FE
=
HM
\left(
1-R,\,
1-P,\,
1-\min(TR,1/TR),\,
1-ES_{\text{entail}},\,
TE
\right)
\]

其中 harmonic mean 为：

\[
HM(z_1,\dots,z_n)
=
\frac{n}{\sum_{j=1}^{n}\frac{1}{z_j}}
\]

展开：

\[
FE
=
\frac{5}{
\frac{1}{1-R}
+
\frac{1}{1-P}
+
\frac{1}{1-\min(TR,1/TR)}
+
\frac{1}{1-ES_{\text{entail}}}
+
\frac{1}{TE}
}
\]

**各参数含义：**

| 参数 | 含义 |
|---|---|
| \(R\) | forget set 上的 ROUGE-L recall。遗忘时希望低，FE 用 \(1-R\)。 |
| \(P\) | ground-truth answer 条件概率，遗忘时希望低，FE 用 \(1-P\)。 |
| \(TR\) | Truth Ratio，比较模型给错误扰动答案与正确 paraphrased answer 的概率关系。 |
| \(\min(TR,1/TR)\) | 对 Truth Ratio 对称化。 |
| \(1-\min(TR,1/TR)\) | FE 中 Truth Ratio 子项。 |
| \(ES_{\text{entail}}\) | Entailment Score，模型输出是否蕴含 ground-truth answer，遗忘时希望低，FE 用 \(1-ES_{\text{entail}}\)。 |
| \(TE\) | Token Entropy，衡量输出多样性，不取 1-TE。 |
| \(HM\) | 调和平均。 |

---

## 2. FQ

**指标：**  
FQ，Forget Quality，遗忘质量。

**意义：**  
衡量 unlearned model 在 forget set 上的行为是否接近 **retain-only model**。p-value 越高，越难区分两者，遗忘越好。  

**公式：**

经验分布：

\[
F_U(z)
=
\frac{1}{n}
\sum_{i=1}^{n}
\mathbf 1[TR_i^U \le z]
\]

\[
F_R(z)
=
\frac{1}{m}
\sum_{j=1}^{m}
\mathbf 1[TR_j^R \le z]
\]

KS 统计量：

\[
D_{n,m}
=
\sup_z
|F_U(z)-F_R(z)|
\]

Forget Quality：

\[
FQ
=
p_{\text{KS}}(D_{n,m})
\]

如果论文表使用 log scale：

\[
FQ_{\text{table}} = \log(FQ)
\]

**各参数含义：**

| 参数 | 含义 |
|---|---|
| \(TR_i^U\) | unlearned model 在 forget set 第 \(i\) 个样本上的 Truth Ratio |
| \(TR_j^R\) | retain-only model 在 forget set 第 \(j\) 个样本上的 Truth Ratio |
| \(\mathcal T_U\) | unlearned model Truth Ratio 分布样本 |
| \(\mathcal T_R\) | retain-only model Truth Ratio 分布样本 |
| \(F_U(z), F_R(z)\) | 两组 Truth Ratio 的经验 CDF |
| \(D_{n,m}\) | 两个经验 CDF 的最大差值 |
| \(p_{\text{KS}}\) | two-sample KS test 的 p-value |
| \(FQ\) | Forget Quality 指标，越高越好 |
| \(FQ_{\text{table}}\) | log scale 表示的 FQ |

---

## 3. ES_unlearn

**指标：**  
ES_unlearn，即 forget / unlearn set 上的 **Extraction Strength**。

**意义：**  
衡量模型在需要遗忘的数据上剩余可抽取知识的程度。越低表示遗忘越好。  

**公式：**

单样本 Extraction Strength：

\[
ES(x,y)
=
1
-
\frac{1}{|y|}
\min_k
\{ k \mid f_\theta(x,y_{<k}) = y_{>k} \}
\]

平均 forget set：

\[
ES_{\text{unlearn}}
=
\frac{1}{|D_F|}
\sum_{(x,y)\in D_F} ES(x,y)
\]

**各参数含义：**

| 参数 | 含义 |
|---|---|
| \(D_F\) | forget set / unlearn set |
| \(x\) | 问题 prompt |
| \(y\) | ground-truth answer |
| \(|y|\) | 答案 token 数 |
| \(y_{<k}\) | 答案前 \(k\) 个 token |
| \(y_{>k}\) | 答案剩余后缀 |
| \(f_\theta(x,y_{<k})\) | 模型在给定前缀下生成的后缀 |
| \(\min_k\{\cdot\}\) | 能让模型精确生成剩余答案的最短前缀长度 |
| \(ES(x,y)\) | 单样本 Extraction Strength |
| \(ES_{\text{unlearn}}\) | forget set 上平均 Extraction Strength |

---

## 4. ES_retain

**指标：**  
ES_retain，即 retain set 上的 **Extraction Strength**。

**意义：**  
衡量模型在应保留数据上是否仍能恢复答案。越高表示保留效果越好。  

**公式：**

单样本同 ES_unlearn：

\[
ES(x,y)
=
1
-
\frac{1}{|y|}
\min_k
\{ k \mid f_\theta(x,y_{<k}) = y_{>k} \}
\]

平均 retain set：

\[
ES_{\text{retain}}
=
\frac{1}{|D_R|}
\sum_{(x,y)\in D_R} ES(x,y)
\]

**各参数含义：**

| 参数 | 含义 |
|---|---|
| \(D_R\) | retain set |
| \(x\) | retain set 问题 |
| \(y\) | retain set ground-truth answer |
| \(|y|\) | 答案 token 数 |
| \(y_{<k}\) | 前缀 token |
| \(y_{>k}\) | 后缀 token |
| \(f_\theta(x,y_{<k})\) | 模型生成的后缀 |
| \(\min_k\{\cdot\}\) | 能让模型 exact match 剩余答案的最短前缀长度 |
| \(ES(x,y)\) | 单样本 Extraction Strength |
| \(ES_{\text{retain}}\) | retain set 上平均 Extraction Strength |